<div class="room-show-container">
  <%= form_for @home_listing, url: wizard_path, method: :put do |f|%>
    <%= f.label :place_type %>
    <%= f.select :place_type, ['Entire Place', 'Private Room', 'Shared Room'] %>
    <%= f.submit "next" %>
  <% end %>
</div>
